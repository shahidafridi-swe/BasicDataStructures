  // int pos;
    // cin >> pos;
    // if (pos < 0 || pos >= size(head))
    //     cout << "INVALID INDEX" << endl;
    // else if (pos == 0)
    //     delete_head(head);
    // else if (pos == size(head) - 1)
    //     delete_tail(tail);
    // else
    //     delete_at_position(head, pos);